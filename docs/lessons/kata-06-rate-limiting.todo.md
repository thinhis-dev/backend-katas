# Kata 06 — Rate limiting

- **Week:** 6
- **Status:** todo
- **Branch (when you start):** `kata-06-rate-limiting`  (off `main`)
- **Spec:** written when you begin — `test/katas/kata-06-*.e2e-spec.ts`

---

## Anchor kata (input → required behavior)

Token-bucket in Redis → 100 req/min/user → `429` + `Retry-After`.

**New infra it forces:** — (Redis).

---

> Stub. Start this kata and the teacher fills in the full concept brief,
> acceptance criteria, and test cases here (and writes the failing spec).
> Ask: *"start kata 06"*.
